import 'package:flutter/material.dart';

class TripManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Trip Management')),
      body: Center(child: Text('Manage Trips Here')),
    );
  }
}

